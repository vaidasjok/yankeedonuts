<%= form_for(gallery) do |f| %>

  <%= f.label :title, "Galerijos pavadinimas" %>
  <%= f.text_field :title, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>
