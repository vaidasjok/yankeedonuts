<% provide(:title, 'Vartotojai') %>
<% provide(:page_title, 'Vartotojai') %>


<%= will_paginate @users, :previous_label => 'Atgal', :next_label => 'Sekantis' %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 50 %>
      <%= link_to user.name, user %>
      <% if current_user.admin? && current_user != user %>
      | <%= link_to "trinti", user, method: :delete,
                                  data: { confirm: "Ar esi tikras dėl trynimo?" } %>
  	  <% end %>
    </li>
  <% end %>
</ul>

<%= will_paginate @users, :previous_label => 'Atgal', :next_label => 'Sekantis' %>