<p id="notice"><%= notice %></p>

<h1>Donuts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @donuts.each do |donut| %>
      <tr>
        <td><%= donut.title %></td>
        <td><%= donut.description %></td>
        <td><%= link_to 'Show', donut %></td>
        <td><%= link_to 'Edit', edit_donut_path(donut) %></td>
        <td><%= link_to 'Destroy', donut, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Donut', new_donut_path %>
