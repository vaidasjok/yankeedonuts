<%= form_for(donut, :html => {:multipart => true}) do |f| %>
  <% if donut.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donut.errors.count, "error") %> prohibited this donut from being saved:</h2>

      <ul>
      <% donut.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :photo %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
